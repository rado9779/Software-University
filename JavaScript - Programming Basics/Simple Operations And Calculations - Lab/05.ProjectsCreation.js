function projectsSolve(architectName, projectsCount){

       let hoursToCreateProjects = projectsCount * 3;
       console.log(`The architect ${architectName} will need ${hoursToCreateProjects} hours to complete ${projectsCount} project/s.`);
}
